#ifndef DUVAL_H
#define DUVAL_H

#include <cstdio>
#include <iostream>
#include <vector>
#include <map>

using namespace std;

vector<size_t> duval(unsigned char* str, size_t n, size_t &max);

int duval_break(unsigned char* str, size_t n);

#endif
